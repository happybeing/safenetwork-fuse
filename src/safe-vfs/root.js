/**
 * vfsHandler for the root mount point ('/')
 *
 */
class RootHandler {
/**
 * [constructor description]
 * @param {[type]} Safenetwork    SAFE API
 * @param {[type]} safePath       mounted path (always '/') (e.g. _publicNames, _public)
 * @param {[type]} mountPath      where safePath appears relative to filesystem mount point
 * @param {[type]} lazyInitialise [description]
 */
  constructor (Safenetwork) {
    try {
      throw new Error('TODO implement rootHandler')
    } catch (err) {
      throw err
    }
  }

  getHandlerFor(Safenetwork) {
    try {
      throw new Error('TODO implement rootHandler')
    } catch (err) {
      throw err
    }
  }
}

module.exports = RootHandler
